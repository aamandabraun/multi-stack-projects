#include <stdio.h>

int main(){
    char letra = 'a';

    printf("---Conversor ASCII ---\n");
    printf("A letra escolhida foi: '%c'\n", letra);
    printf("O valor de '%c' na tabela ASCII e: %d\n", letra, letra);

    return 0;
}